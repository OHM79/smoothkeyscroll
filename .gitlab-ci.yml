pages:
  stage: deploy
  image: node:5
  cache:
    paths:
    - chrome-extension/node_modules/
  script:
  - cd chrome-extension
  - npm install --unsafe-perm
  - npm run build
  - cp -r dist/ ../public
  artifacts:
    paths:
    - public
  only:
  - page
